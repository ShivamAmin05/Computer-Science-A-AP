class TimeToSeconds{
  public void tToS()
    {
      int hours = 1;
      int minutes = 28;
      int seconds = 42;
      int totalSeconds = (hours * 3600) + (minutes * 60) + seconds;
    System.out.println(hours + " hours plus " + minutes + " minutes plus" + seconds + " is equal to " + totalSeconds + " seconds");
    }
}